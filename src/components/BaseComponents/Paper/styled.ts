import { styled } from '@mui/material/styles';

export const SWrapper = styled('div')({
  width: '100%',
  height: '100%',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  boxShadow: '0px 0px 5px 2px #8080807a',
  border: '1px solid #8080807a',
  borderRadius: '10px',
  overflow: 'scroll',
});
